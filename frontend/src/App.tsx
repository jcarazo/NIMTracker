import { LandingPage } from './pages/LandingPage'

// No router yet -- only the landing page exists this phase. Phase 5
// introduces actual routing when Models/ModelDetail/Executions get
// built (see CLAUDE.md, "Current state").
function App() {
  return <LandingPage />
}

export default App
