[project]
name = "nimtracker-backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.62.0",
    "psycopg[binary]>=3.3.5",
    "httpx>=0.28.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
]

[tool.setuptools]
packages = ["nimtracker"]
